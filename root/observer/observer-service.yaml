apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: observer-service
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/Dan9191/infra.git
    targetRevision: main
    path: apps/observer/observer-service
  destination:
    server: https://kubernetes.default.svc
    namespace: observer
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
