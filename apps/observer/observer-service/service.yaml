apiVersion: v1
kind: Service
metadata:
  name: observer-service
  namespace: observer
  labels:
    app: observer-service
spec:
  type: ClusterIP
  ports:
    - port: 8033
      targetPort: 8033
      protocol: TCP
      name: http
  selector:
    app: observer-service
