apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: mos-hack-graduation-gateway
  namespace: graduation-gateway
spec:
  secretName: mos-hack-tls
  dnsNames:
    - api.mos-hack.ru
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer