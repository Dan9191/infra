apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: mos-hack-graduation-frontend
  namespace: graduation-frontend
spec:
  secretName: mos-hack-tls
  dnsNames:
    - api.mos-hack.ru
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer