apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: monitor-mos-hack
  namespace: observer
spec:
  secretName: monitor-tls
  dnsNames:
    - monitor.mos-hack.ru
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
