apiVersion: v1
kind: Service
metadata:
  name: rag-service
  namespace: graduation-rag
  labels:
    app: rag-service
spec:
  type: ClusterIP
  ports:
    - port: 8094
      targetPort: 8094
      protocol: TCP
      name: http
  selector:
    app: rag-service